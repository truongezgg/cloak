{
  "name": "cloak",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20.12.0"
  },
  "bin": {
    "cloak": "dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "dev": "tsx src/cli.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.3.0",
    "@types/node": "^20.0.0",
    "hash-wasm": "^4.12.0"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
